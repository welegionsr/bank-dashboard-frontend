import { Roboto } from 'next/font/google';
import { Jockey_One } from 'next/font/google';


export const roboto = Roboto({ subsets: ['latin'], weight: ['400', '500', '700'], preload: true });
export const jockeyOne = Jockey_One({ subsets: ['latin'], weight: '400', preload: true });
